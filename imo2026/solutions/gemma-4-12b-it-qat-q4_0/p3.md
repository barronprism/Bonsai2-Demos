To find the largest value $c$ that Liu can guarantee, we analyze the game for small values of $n$ and then generalize. The points are marked on the stick (the interval $[0, 1]$), and the stick is cut into pieces. The players then take turns claiming the largest available piece. Let the sorted lengths of the pieces be $s_1 \ge s_2 \ge s_3 \ge s_4 \ge \dots \ge s_N$, where $N$ is the total number of pieces. Since Liu goes first, he will receive the pieces $s_1, s_3, s_5, \dots$. The sum of the lengths of all pieces is $\sum_{i=1}^N s_i = 1$.

For $n=1$, Liu marks $x_1$. Xiang marks $y_1$. The stick is divided into $N=3$ pieces with lengths $s_1, s_2, s_3$. Liu gets $s_1+s_3$, and Xiang gets $s_2$. Since $s_1 \ge s_2 \ge s_3$, Liu's share is $s_1+s_3 = 1-s_2$. To maximize his guarantee, Liu chooses $x_1$ to minimize the maximum possible value of $s_2$. If Liu marks $x_1=1/3$, and Xiang marks $y_1$ anywhere, it can be shown that $s_2 \le 1/3$, so Liu's share is at least $1-1/3 = 2/3$.

For $n=2$, Liu marks $x_1, x_2$. Xiang marks $y_1, y_2$. The number of pieces $N$ is at most 5. Liu's share is $s_1+s_3+s_5$ (if $N=5$) or $s_1+s_3$ (if $N=3$ or $N=4$). For $N=5$, the lengths are $s_1, s_2, s_3, s_4, s_5$. Liu's share is $1-(s_2+s_4)$. To maximize this, Liu wants to minimize $s_2+s_4$. By marking $x_1=1/3, x_2=2/3$, the stick is divided into three regions of length $1/3$. Xiang's $n=2$ marks will divide these into 5 pieces. At least one piece in each region will have a length at most $1/3$. It can be shown that the maximum $s_2+s_4$ Xiang can achieve is $2/5$, so Liu can guarantee $c = 1 - 2/5 = 3/5$.

Generalizing this, if Liu marks points $x_1, \dots, x_n$ at $\frac{1}{n+1}, \frac{2}{n+1}, \dots, \frac{n}{n+1}$, the stick is divided into $n+1$ regions of length $\frac{1}{n+1}$. Xiang's $n$ points will divide these regions into $N \le 2n+1$ pieces. In each region, the pieces $s_i$ sum to $\frac{1}{n+1}$. Liu's share is $s_1+s_3+\dots+s_{2k+1}$ (where $2k+1 = N$ or $N$ is even and he takes half). It can be shown that by this strategy, Liu can guarantee a total length of at least $c = \frac{n+1}{2n+1}$.

Alternatively, considering the symmetry of the game and the property of the largest pieces, Liu's strategy of choosing points to partition the stick into $n+1$ equal segments of length $\frac{1}{n+1}$ ensures that Xiang cannot make more than half of the total length $1$ for his own pieces. The maximum value $c$ that Liu can guarantee is $\frac{n+1}{2n+1}$.